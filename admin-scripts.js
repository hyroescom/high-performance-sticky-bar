/**
 * Admin scripts for Lightweight High Performance Sticky Bar
 * 
 * Handles initialization of WordPress color picker and other admin UI functionality
 */
jQuery(document).ready(function($) {
    // Initialize WordPress color picker for all color fields
    $('.color-picker').wpColorPicker();
});
